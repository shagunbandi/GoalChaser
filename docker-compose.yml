version: '3.8'

# GCS service account: place key file at ./gcs-key.json (or set GCS_KEY_FILE_HOST).
# Mounted read-only at /app/gcs-key.json. Ensure the file exists and is readable (e.g. chmod 644).
services:
  nitya:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        - NEXT_PUBLIC_FIREBASE_API_KEY=${NEXT_PUBLIC_FIREBASE_API_KEY}
        - NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=${NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN}
        - NEXT_PUBLIC_FIREBASE_PROJECT_ID=${NEXT_PUBLIC_FIREBASE_PROJECT_ID}
        - NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=${NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET}
        - NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=${NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID}
        - NEXT_PUBLIC_FIREBASE_APP_ID=${NEXT_PUBLIC_FIREBASE_APP_ID}
    container_name: nitya
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    expose:
      - "3000"
    env_file:
      - .env
    environment:
      - NODE_ENV=production
      # Path inside container where GCS key is mounted (see volumes)
      - GCS_KEY_FILE=/app/gcs-key.json
      - GCS_BUCKET_NAME=${GCS_BUCKET_NAME}
    volumes:
      # Mount GCS service account key so upload API can read it (path relative to compose file)
      - ${GCS_KEY_FILE_HOST:-./gcs-key.json}:/app/gcs-key.json:ro
    networks:
      - nitya_network
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.nitya.entrypoints=websecure"
      - "traefik.http.routers.nitya.rule=Host(`nitya.geekynavigator.com`)"
      - "traefik.http.routers.nitya.tls=true"
      - "traefik.http.routers.nitya.tls.certresolver=cloudflare"
      - "traefik.http.services.nitya.loadbalancer.server.port=3000"
      - "traefik.docker.network=nitya_network"

networks:
  nitya_network:
    external: true

